{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Current Day",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "args": [],
            "cwd": "${fileDirname}" 
        }
    ]
}